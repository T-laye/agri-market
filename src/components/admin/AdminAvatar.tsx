import Avatar from "@/components/Avatar";

/** Thin re-export so admin call sites don't need to know this moved to a
 * shared component once the public marketplace needed the same avatar
 * rendering (farmer cards / detail page). */
export default Avatar;
